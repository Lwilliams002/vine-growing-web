# Supabase project used for the announcements admin (/admin).
# Both values are public-safe; access is controlled by Row Level Security.
# Supabase dashboard -> Project Settings -> API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
